// API Configuration
const API_CONFIG = {
  // Set to true to completely bypass API calls and use mock data only
  USE_MOCK_DATA: true,
  BASE_URL: 'http://localhost:8000',
  TIMEOUT: 10000,
  // Mock data delay in ms (to simulate network latency)
  MOCK_DELAY: 300
};

export default API_CONFIG;